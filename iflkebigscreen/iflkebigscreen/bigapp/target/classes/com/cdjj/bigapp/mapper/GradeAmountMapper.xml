<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cdjj.bigapp.mapper.GradeAmountMapper">
   <!--
   public List<CommentPojo> selectByNumAreaAmount(Integer userGrade);
   -->
    <select id="selectByNumAreaAmount" resultType="CommentPojo">
        select province_name as 'name'
             ,sum(order_amount) as 'value'
        from ads_trade_level_tb
        where user_level = #{userGrade}
        group by province_name
        order by sum(order_amount) desc
    </select>
    <!--
    public List<CommentPojo02> selectByNumAreaCount(Integer userGrade);
    -->
    <select id="selectByNumAreaCount" resultType="CommentPojo02">
        select sku_name as 'name'
             ,count(sku_name) as 'value'
        from ads_trade_level_tb
        where user_level = #{userGrade}
        group by sku_name
        order by count(sku_name) desc
    </select>
</mapper>